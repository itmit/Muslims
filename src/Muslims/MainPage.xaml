<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Muslims"
             x:Class="Muslims.MainPage"
             Title="Menu">
   <MasterDetailPage.Master>
        <ContentPage Title="Menu" BackgroundImage="menu_bg.jpg" BackgroundColor="Transparent">
     <StackLayout Spacing="0">
     <Image Source="logo.png" Margin="0,0,0,-50"/>
      <ListView x:Name="ListMenu" ItemSelected ="ListMenu_ItemSelected">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <StackLayout Orientation="Vertical" VerticalOptions="Center">
                  <StackLayout Orientation="Horizontal" Margin="15,0,0,0">
                  <Image Source="{Binding ImagePath}"/>
                  <Label Text="{Binding MenuTitle}"
                   TextColor="#ffffff" VerticalOptions="End" FontSize="20" Margin="10,0,0,0"/>
                </StackLayout>
              </StackLayout>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
     </StackLayout>
        </ContentPage>
  </MasterDetailPage.Master>
</MasterDetailPage>
